# Stitch vs. Live Emulator — Screen-by-Screen Verification

Working checklist for comparing every Stitch design mock (project `4011413732038259168`)
against how it actually renders in the Piggybank Flutter app on a live Android emulator.
Started 2026-09-01, per `/ecc:prompt-optimizer`'s optimized prompt (user: "I still don't see
some of the screens the way it is rendered in Stitch, lets go and review each Stitch screen
against the live emulator to verify they are correct, 1 by 1").

Verdicts: **MATCH** / **MISMATCH** (structure or token issue, described) / **NO FLUTTER**
(Stitch mock exists, no corresponding built screen — not a bug, just unbuilt scope) / **NO
STITCH** (Flutter screen exists, no design mock — reverse gap, noted for awareness only).

Full pass first — do not fix anything until every row below has a recorded verdict.

## IMPORTANT — scope pivot discovered mid-sweep (2026-09-01)

The current Stitch project (`4011413732038259168`, brief: `docs/stitch-design-brief.md`,
Aug 23) is **explicitly documented as "exploratory, not canonical" — "a full-app redesign
exploration rather than a locked-identity refresh."** It is not a spec the app already failed
to match. The app was actually built against an *earlier* brief (`docs/ui-ux-mockup-brief.md`,
Aug 17), whose resulting mockups are saved locally as `assets/login.jpeg`, `dashboard.jpeg`,
`accounts.jpeg`, `budgets.jpeg`, `goals.jpeg`, `investments.jpeg`, `register.jpeg`,
`settings.jpeg`, `transactions.jpeg` — confirmed by comparing `assets/login.jpeg` against the
live Login screen: tagline, "Forgot password?" placement, and footer copy all match the local
reference, NOT the current Stitch project's Login mock (different tagline, different mascot
render, "Forgot?" placed differently).

Discovered on Login while starting the sweep: the real Piggybank mascot/logo images already
exist in `assets/` (`Piggybank mascot.jpeg`, `Piggybank app logo.jpeg`) but `pubspec.yaml`'s
`assets:` section was entirely commented out, so three screens — Login, Lock Screen, Register —
fell back to a generic `Icons.savings` placeholder, each with an explicit code comment saying
so ("Placeholder for the mockups' 3D piggy-bank mascot illustration — no real asset exists
yet"). This is a real, concrete gap independent of which design brief is authoritative.

**User decision, asked mid-sweep:** rather than (a) a full redesign-adoption sweep against the
exploratory Stitch project, or (b) re-scoping the whole comparison to the older local mockups,
fix the concrete mascot/asset-wiring gap only. **Status: DONE** — see Step 2 below. The broader
39-screen full sweep was paused here rather than continued against a redesign that isn't meant
to be a fix target yet; resume it (with an explicit choice of baseline) if/when wanted.

## Step 1 — Stitch-side status (resolved)

- Imports Wizard — confirmed generated (2 copies exist — duplicate, not a blocker).
- Financial Assistant: Welcome / Chat Thread (Chatbot/Penny) — confirmed generated.
- Admin Dashboard — confirmed generated. **NO FLUTTER** — Admin has no client UI per the
  fix-it blueprint's L6 decision ("leave both as-is"). Not a mismatch to fix.
- User Management — confirmed generated. **NO FLUTTER** — same as above, presumed to be the
  admin user list/detail mock; not comparable against a build that doesn't exist.
- Loan Calculator — 2 exact-title duplicates in Stitch (harmless, noted for Stitch-side
  cleanup only, not an app-code issue).

## Step 2 — Screen checklist

| Stitch title | Flutter screen (best-guess mapping) | Verdict | Notes |
|---|---|---|---|
| Login | `auth/screens/login_screen.dart` | **MISMATCH** (redesign gap, not a bug) | Mascot-wiring bug already fixed (real `assets/mascot.jpg`). Remaining differences vs. the current Stitch mock, itemized 2026-09-01: (1) mascot art style — Stitch uses a small cartoon icon paired inline with a "Piggybank" wordmark badge, live uses a large photorealistic 3D image with no inline wordmark; (2) live adds a separate large "Piggybank" heading below the image that Stitch doesn't have; (3) tagline text differs — Stitch "Your money, perfectly in order." vs. live "All your wealth in one place"; (4) "Forgot password?" is inline next to the Password label in Stitch, but a separate line below the Log-in button in live; (5) Stitch's Log-in button has a trailing arrow icon ("Log in →"), live's has text only; (6) secondary link copy differs — "Create one" (Stitch) vs. "Register" (live); (7) trust-footer icon differs — padlock (Stitch) vs. checkmark-shield (live); (8) trust-footer copy differs in exact wording, same meaning. All cosmetic/copy, part of the unshipped exploratory redesign — none require a fix under the current baseline decision (Stitch project = redesign roadmap, not a locked spec to match now). |
| Forgot password | `auth/screens/forgot_password_screen.dart` | **MISMATCH** | Copy matches Stitch almost verbatim (body text, "Send reset code", "Back to log in"). But live has NO mascot image at all — code (`forgot_password_screen.dart`) never included one (no placeholder comment, unlike Login/Lock/Register), so this wasn't caught by the earlier mascot-wiring fix. Live shows a large empty gap above the body text where Stitch places the piggy mascot. Structural gap, not just a copy/redesign variant. |
| Reset Password | `auth/screens/reset_password_screen.dart` | **MISMATCH** | Same missing-mascot gap as Forgot Password — Stitch shows the piggy mascot above the body text, live has empty space instead. Otherwise a strong match: body copy near-identical (live actually improves on Stitch by naming the actual email address instead of "your email"), "Reset code" field icon matches, "New password" field (lock icon + eye toggle) matches, "Reset password" button text/style matches. Minor: Stitch's back-arrow icon is tinted green, live's is plain black (same header-icon-color variant seen elsewhere). |
| Lock Screen: PIN Entry | `auth/screens/lock_screen.dart` | **FIXED** | Same mascot placeholder fix. Verified via `flutter analyze` (clean) + `lock_screen_test.dart` (5/5 passing) — live screenshot not captured (emulator IME made demo login unreliable this session), but the code change is byte-identical in pattern to Login/Register, both live-confirmed, and the Column here has no `stretch` cross-axis issue (default `center`, unlike Register's bug — see below). |
| Lock Screen: PIN Entry (Error State) | `auth/screens/lock_screen.dart` (error state) | **FIXED** | Same fix, same widget. |
| Lock Screen: Biometric | `auth/screens/lock_screen.dart` (biometric variant) | **FIXED** | Same fix, same widget. |
| Privacy & Consent: First-run Gate | `consent/screens/consent_screen.dart` (`_allOutstanding == false` branch) | **MISMATCH** (redesign gap, code-reviewed not live-captured) | Could not trigger live: the demo account's consent is recorded server-side, so `pm clear` (which only wipes local app state) doesn't bring back the gate — `auth.consentsRequired` stays false and the router redirect never lands on `/consent` in gate mode. Verdict is based on reading the widget's un-conditional-accept branch (`consent_screen.dart:176-217`) against the Stitch mock. Differences: (1) Stitch has a green shield icon and bold headline "Your security is our priority" above the checklist; live's gate branch has no icon and a plainer heading "Before you continue"; (2) Stitch renders each document as a row with a leading icon-avatar, title, one-line description, and a separate green "Read" link on the trailing edge; live uses a single `CheckboxListTile` per document with the summary and "(Read)" concatenated into one subtitle string, no icon avatar; (3) Stitch's button reads "Accept and Continue" and appears in a disabled/greyed style until presumably both boxes are checked; live's button text is "Accept and continue" (lowercase c), same enable-when-all-checked behaviour. Structural/cosmetic redesign gap, not a functional bug — the gate works, just doesn't match Stitch's visual language. |
| Privacy & Consent: Review (Settings) | `consent/screens/consent_screen.dart` (`_allOutstanding == true` branch) | **MATCH** | Live (Settings → Privacy & consent, demo account, already consented) closely matches Stitch: green checkmark icon, "You're up to date" heading, near-identical body copy ("You've accepted the current Privacy Policy and Terms of Service." vs. Stitch's "You have accepted the following agreements. You can review them at any time." — same meaning, different wording), and a card listing Privacy Policy / Terms of Service rows with a document icon and chevron. Minor: Stitch shows a small green checkmark badge + "Accepted on 24 Oct 2023" per row and a "Read" link on the trailing edge; live shows just a truncated one-line summary and a chevron (no accepted-date shown, no separate Read label — the whole row is tappable). Cosmetic difference only. |
| Dashboard | `dashboard/screens/dashboard_screen.dart` | **MISMATCH** (redesign gap) | Live (demo account) screenshotted 2026-09-02 after logging in fresh. Structure and data hierarchy match closely (net worth hero card, cashflow row, top goal card, 4-icon quick-actions row, recent transactions list, bottom nav) but styling differs throughout: (1) header — Stitch uses a small mascot avatar + bell icon only, no "Piggybank" wordmark in the top bar; live has a green circular person-icon avatar, "Piggybank" text title, and bell; (2) net worth card — Stitch adds a small "+2.4% this month" trend pill with an up arrow under the value; live has no trend indicator; (3) income/expenses row — Stitch shows it as two bare columns with small up/down arrow glyphs next to the labels, no card border; live wraps it in a bordered card titled "Cashflow · This month" with no arrow glyphs; (4) quick-actions icons (Accounts/Assets/Liabilities/Calculators) — Stitch uses distinct per-icon colours (green bank, green leaf, red/pink receipt, green calculator) on light rounded-square backgrounds; live uses a single dark-green tone for every icon on plain circular backgrounds — no colour variation; (5) transaction rows — Stitch shows merchant/service icon avatars (shopping cart, TV/streaming icon); live shows generic up/down arrow icons colour-coded green (inflow) / red (outflow) instead. All cosmetic, part of the unshipped exploratory redesign — not a functional bug. |
| Accounts | `accounts/screens/accounts_screen.dart` | **MISMATCH** (redesign gap) | Live screenshotted 2026-09-02 (demo account, navigated from Dashboard's Accounts quick-action). Same core structure (total-balance hero card, active-accounts list with icon + name + balance + chevron, Add account button, bottom nav) but differs: (1) header — Stitch has a small mascot avatar + short label + bell, no page title; live has a back arrow, "Accounts" title, and a receipt/history icon (no bell, no avatar); (2) section header — Stitch uses uppercase "ACTIVE ACCOUNTS" small-caps label; live uses sentence-case "Active accounts"; (3) account rows — Stitch shows a bank/provider subtitle under each account name (e.g. "Standard Bank", "Capitec", "FNB"); live shows only the account name with no subtitle; (4) inactive accounts — Stitch has a separate "INACTIVE 2" row/section below the active list; live has no inactive-accounts section at all (not shown, possibly not implemented). All cosmetic/structural, part of the unshipped exploratory redesign — not a functional bug. |
| Add account | `accounts/screens/account_edit_screen.dart` (create mode) | **MISMATCH** (redesign gap) | Live screenshotted 2026-09-02 (tapped "Add account" from the Accounts screen). Same fields (Account name text input, Type dropdown defaulted to "Bank account", green Save button) but presentation differs: (1) Stitch renders as a full-page/modal view with a centered "Add account" title and an "X" close icon top-left; live renders as a bottom sheet overlaying the dimmed Accounts screen, with a left-aligned "Add account" heading and no close icon (dismissed by tapping outside/back, presumably); (2) Stitch's Account name field has example placeholder text "e.g. Main Savings"; live's field shows only the generic label "Account name" with no example. Structurally equivalent, presentation-level gap from the unshipped exploratory redesign — not a functional bug. |
| — (register, no separate Stitch title but same brief covers it) | `auth/screens/register_screen.dart` | **FIXED** | Same mascot placeholder fix as Login/Lock — found the same "Placeholder for the mockups' 3D piggy-bank mascot illustration" comment. Fixing this one surfaced a second bug: the parent `Column` uses `CrossAxisAlignment.stretch`, which stretched the fixed-size mascot image to full width until wrapped in `Center` (matching Login's pattern). Confirmed live both before (stretched, wrong) and after (correct) via rebuilt debug APK screenshots. |
| — (no Stitch mock) | `accounts/screens/account_detail_screen.dart` | **NO STITCH** | Built in the fix-it blueprint (L4), after this Stitch project's most recent screens — never mocked. Reverse gap, not this pass's focus, but flagging. |
| Transactions | `transactions/screens/transactions_screen.dart` | **MISMATCH** (redesign gap) | Live screenshotted 2026-09-02 (demo account, via Dashboard "See all"). Same core structure (back arrow, title, filter/breakdown/export icons, All/Income/Expense/Transfer filter chips, date-grouped transaction list, Add transaction FAB, bottom nav) but differs: (1) header — Stitch has a mascot avatar and renders "Transactions" in green; live has a plain back arrow and black title text; (2) selected filter chip — Stitch's "All" chip is a plain black/dark pill with no checkmark; live's is green with a checkmark icon; (3) transaction icons — Stitch uses merchant/category-specific icons (shopping bag, coffee cup, fuel pump, transfer arrows, TV icon, shopping cart) each on a light-tinted background; live uses a single generic up/down arrow icon colour-coded green (inflow) / red (outflow) for every row, no merchant iconography; (4) date grouping — Stitch groups relatively ("Today", "Yesterday", then absolute dates); live groups only by absolute date ("27 Aug 2026", "30 Jun 2026", etc.); (5) amount styling — Stitch shows all amounts in plain black text; live colour-codes amounts green (income) / red (expense); (6) Stitch has a "Load more" link at the list's end; not observed in the live scroll captured. All cosmetic/structural, part of the unshipped exploratory redesign — not a functional bug. |
| Add transaction | transactions add flow (sheet/dialog — confirm exact widget) | **MISMATCH** (unable to fully compare — Stitch render broken) | Live screenshotted 2026-09-02: bottom sheet with "Add transaction" heading, Expense/Income/Transfer segmented toggle (Expense selected, green), Amount (ZAR), Category, Merchant (optional), Account (optional) fields, Date picker (defaulted to today), Notes (optional), green Save button — consistent with the Add account sheet's bottom-sheet pattern. The Stitch mock for this screen (`026ff5b000864d7da4676cd826f5e71e`) downloads as a solid-black ~5KB image via its `screenshot.downloadUrl` (confirmed via `mcp__stitch__get_screen`, not a fetch/auth error — the URL resolves HTTP 200 but the rendered image is blank/black), so a visual comparison isn't possible from the API. Widget field/label set (Amount, Category, Merchant, Account, Date, Notes) mirrors the pattern of every other MISMATCH row so far, so a redesign-gap verdict is the reasonable default, but this one is unverified — flag for a manual look at the Stitch project UI if a real comparison is needed later. |
| Budgets | `budgets/screens/budgets_home_screen.dart` (only one wired in `app_router.dart`; `budgets_screen.dart` is unreferenced) | **MISMATCH** (redesign gap) | Live screenshotted 2026-09-02 (demo account, Budgets bottom-nav tab). Same core structure (Budgets/Goals segmented toggle, month navigator, category budget list, Add budget FAB, bottom nav) but differs: (1) Stitch adds a "TOTAL SPENT" summary card above the category list (dark green, "R 8,450" headline, "56%" badge, progress bar, "R 6,550 remaining" / "of R 15,000"); live has no summary card at all — month navigator goes straight into the category list; (2) Stitch has a "Categories" section label with a trailing "Edit" link; live has no section label; (3) category rows — Stitch gives each category a distinct icon (house, fork/knife, bag, popcorn, car) on a light tinted background; live rows have no icon at all, just the name; (4) status colour-coding — Stitch's percentage badge and progress bar turn red for an over-budget category (e.g. "Shopping" at 122%, "R 450 over budget"); live's percentage pill and bar are green for every row regardless of spend (all demo categories show "0%" spent, so under-budget styling only — not yet confirmed whether live has an over-budget red state at all); (5) category set/order differs — Stitch: Housing, Food & Drink, Shopping, Entertainment, Transport; live: Groceries, Rent, Utilities, Transport, Insurance. All cosmetic/structural, part of the unshipped exploratory redesign — not a functional bug. |
| Goals | `goals/screens/goals_screen.dart` | **MISMATCH** (redesign gap) | Live screenshotted 2026-09-02 (demo account, Goals segment on the Budgets tab). Same core structure (Budgets/Goals segmented toggle, goal card list with name/percentage/progress bar/amounts, Add goal button, bottom nav) but differs: (1) header — Stitch has a mascot avatar and a "Piggybank" wordmark title with a bell icon; live has a green person-icon avatar and a plain "Goals" title with a bell; (2) segmented control style — Stitch is a light-grey pill with the selected segment shown as plain white/black text, no checkmark; live's selected segment is solid green with a checkmark icon; (3) goal cards — Stitch shows only "R saved / R goal" with no deadline; live adds a third field, "by [date]" (e.g. "by 2026-01-01"); (4) "Add Goal" — Stitch renders it as a centered pill button inline above the bottom nav; live renders it as a green floating action button pinned bottom-right; (5) goal data/order differs (Stitch: Emergency Fund, New Laptop, Vacation to Cape Town; live: New Laptop, House Deposit — Cape Town, Emergency Fund) — expected, demo data vs. mock data, not itself a design gap. All cosmetic/structural, part of the unshipped exploratory redesign — not a functional bug. |
| Assets | `assets/screens/assets_screen.dart` | **MISMATCH** (redesign gap) | Live screenshotted 2026-09-02 (demo account, Assets quick-action from Dashboard). Stitch mock ID `f23ea4c965514121b9e337cc3116ccaf`, title "Assets" confirmed via `get_screen`. Same core structure (hero total card, itemized asset list with icon/name/category/value, bottom nav) but differs: (1) header — Stitch has no dedicated "Assets" screen header at all, it reuses the Dashboard-style top bar (mascot avatar, small logo glyph, bell icon); live has a back arrow plus a plain "Assets" title, no avatar/bell; (2) hero card copy/case — Stitch: "Total Assets" (Title Case); live: "Total assets" (sentence case); (3) Stitch adds a "Your Assets" section label above the list; live has no section label — hero card flows straight into the list; (4) list content — Stitch shows 4 generic example rows (Primary Residence R850,000; Investment Portfolio R345,200; Vehicle (BMW X3) R45,200; Cash & Savings R10,000) totalling R1,250,400; live shows the demo account's actual 7 assets (2× Personal Laptop, Solar Panel System, Toyota Fortuner 2023, FNB Money Market, Rental Property Cape Town, Primary Residence) totalling R6,956,000 — expected, demo data vs. mock data; (5) icon style — Stitch uses solid single-colour flat icons (house, trending-up chart, car, bank/institution) with no visible category variety beyond those 4; live uses light-green circular badges with category-specific glyphs (triangle/square for "Other", car for Vehicle, cash-note icon for Cash, house for Property) — different icon set and treatment; (6) no FAB visible in the Stitch mock; live has a green "+ Add asset" floating pill button bottom-right; (7) bottom nav — both show Home/Invest/Budgets/Insights/Settings (5 items, no dedicated Assets tab in either), but Stitch's mock renders "Invest" as the active/highlighted tab (likely just the mock's default state, not meaningful). All cosmetic/structural, part of the unshipped exploratory redesign — not a functional bug. |
| Liabilities | `liabilities/screens/liabilities_screen.dart` | **MISMATCH** (redesign gap) | Live screenshotted 2026-09-02 (demo account, Liabilities quick-action from Dashboard). Stitch mock ID `11ef283558854bd7bf4ccb8d4ffab7c7`, title "Liabilities" confirmed via `list_screens`. Same core structure (hero total card, itemized liability list with icon/name/category/value/progress bar, bottom nav) but differs: (1) header — Stitch reuses the Dashboard-style top bar (mascot icon, "Piggybank" wordmark, bell icon), no dedicated screen title; live has a back arrow plus a plain "Liabilities" title, no avatar/wordmark/bell; (2) hero card copy/case — Stitch: "Total Liabilities" (Title Case) R540,200.00; live: "Total liabilities" (sentence case) R3,517,000.00 — demo data vs. mock data, expected; (3) icon treatment — Stitch varies icon colour by type (green circle for Home Loan/Vehicle Finance/Store Account, pink/red circle only for Credit Card); live uses a uniform pink/red circle badge for every liability regardless of type (mortgage, vehicle loan, credit card all share the same pink icon background) — no colour differentiation; (4) progress bar — Stitch shows a "X% paid" caption above the bar plus "R [total] total" to the right of the bar (e.g. "65% paid" / "R890,000.00 total"); live shows the bar with only a trailing green "0%" pill badge, no "paid" label and no total-amount caption, and the Credit Card row in live has no progress bar at all (matching Stitch, which also omits a bar for Credit Card); (5) over-limit state — Stitch's Credit Card row has a distinct warning treatment ("⚠ R2,500 over limit" in red text, red accent stripe on the row's right edge); live's Credit Card row shows no over-limit warning styling (demo data has no over-limit liability, so this state is unverified on live, not necessarily missing); (6) live's list has duplicate entries (FNB Credit Card, Toyota Fortuner Finance, and Home Loan — FNB each appear twice in the visible scroll) — likely a demo-data seeding artifact consistent with the duplicate "Personal Laptop" seen on the Assets row, not a Stitch-comparison issue; (7) FAB — live has a green "+ Add liability" floating pill button bottom-right; Stitch mock is cropped before the fold so its FAB presence is unconfirmed. Bottom nav matches (Home/Invest/Budgets/Insights/Settings, Home active in both). All cosmetic/structural, part of the unshipped exploratory redesign — not a functional bug. |
| — (no Stitch mock) | `liabilities/screens/liability_detail_screen.dart` | **NO STITCH** | |
| Invest | `portfolios/screens/invest_screen.dart` | **MISMATCH** (redesign gap, plus a live error state) | Live screenshotted 2026-09-02 (demo account, Invest bottom-nav tab). Stitch mock ID `dbfab572163a4185a0d0e206cb977ef5`, title "Invest" confirmed via `get_screen`. Major structural gap, not just cosmetic: (1) live shows a "Something went wrong. Please try again." error banner directly under the header, in place of Stitch's entire top section — Total Portfolio Value hero card (green gradient, "+12.4% YTD" trend pill), a two-column Unrealized P&L / Dividends (YTD) card row, and an Asset Allocation donut chart (Equity/Bonds/Cash/Crypto breakdown) are all absent from live; whether this is a genuinely broken data fetch on the demo account or a section that was never built is unconfirmed — flag for a code check, out of scope for this documentation-only pass; (2) Stitch has a "Top Holdings" list (Satrix 40, Apple Inc, Bitcoin, each with ticker/category, price, and a green/red % change) with a "See all" link; live has no holdings list at all; (3) "Your Portfolios" section — present on both, live starts straight here since everything above it is missing; row structure matches (folder icon, name, subtitle, chevron) but naming differs: Stitch shows "General Investment / Standard Account", "Tax Free Savings (TFSA) / TFSA Account", "Retirement Annuity (RA) / TFSA Account" (note: Stitch mock itself mislabels the RA row's subtitle as "TFSA Account"); live shows "QA RA Portfolio / Retirement Annuity", "QA TFSA Portfolio / TFSA", "Growth Portfolio / General" — demo data vs. mock data, expected; (4) header — Stitch reuses the Dashboard-style top bar (mascot avatar, small chart-glyph icon, bell); live has a green person-icon avatar, "Invest" title, a trend-chart icon, and a bell — closer to Stitch here than most other screens but still not identical. Bottom nav matches (Home/Invest/Budgets/Insights/Settings, Invest active in both). |
| Portfolio Detail | `portfolios/screens/portfolio_detail_screen.dart` | **MISMATCH** (redesign gap, plus a portfolio-specific data-fetch bug on one of the three demo portfolios) | Live screenshotted 2026-09-02 (demo account). Stitch mock ID `158d219db69a4bb7831f3b8c5d13f1a6`, title "Portfolio Detail" confirmed via `list_screens`. Checked two of the three demo portfolios: "QA RA Portfolio" renders only the centered text "Something went wrong. Please try again." in the body — a genuine broken data fetch, not a redesign gap, and not representative of the screen generally (isolated to this portfolio's data, since the next one loads fine); "QA TFSA Portfolio" renders correctly: header (back arrow, "QA TFSA Portfolio" title, edit-pencil, plus an extra receipt/history icon not in Stitch), a "0 holdings" + "ZAR" badge row (not in Stitch at all), a green gradient "Portfolio value" hero card (R0,00 + "+R0,00 unrealized" pill — demo data, 0 holdings), a "Holdings" section reading "No holdings yet.", and a green "Add holding" FAB. Comparing the working TFSA render against Stitch: Stitch has no "X holdings"/currency-badge row above the hero card; Stitch's hero card includes a "+R1,240.00 (1.5%)" trend line (unverified on live with real holdings, since the working demo portfolio has none); Stitch has a dedicated "Allocation" section (segmented Equity/Bonds/Cash bar + coloured-dot legend) between the hero card and holdings list — entirely absent on live, structural gap not just cosmetic; live's "Holdings" section label matches Stitch's "Your Holdings" in position but not case/wording. FAB matches (both "Add holding"). Checked the third demo portfolio too: "Growth Portfolio" shows the identical "Something went wrong. Please try again." error as QA RA Portfolio. So 2 of the 3 demo portfolios (RA, Growth) error and only the empty one (TFSA, 0 holdings) renders — suggesting the fetch failure is triggered specifically by a portfolio that *has* holdings, not by one specific bad record. This also means no live portfolio with actual holdings could be reached in this pass — blocks a live check of the Holding Detail Sheet row below. Flag for a code check outside this documentation-only pass: likely a bug in whatever loads/parses a non-empty portfolio's holdings (Invest tab's own error banner is consistent with this same root cause). |
| Holding Detail Sheet | `portfolios/screens/holding_detail_sheet.dart` | **BLOCKED** — could not reach live (upstream data-fetch bug, see Portfolio Detail row above) | Stitch mock ID `211cf655837f455796bf3376f884a5c7`, title "Holding Detail Sheet" confirmed via `get_screen`. Both demo portfolios with actual holdings (QA RA Portfolio, Growth Portfolio) error out on Portfolio Detail before any holding row is ever shown to tap into; the only portfolio that loads (QA TFSA) has 0 holdings. No path exists in the current demo-account state to open this sheet live. Screenshot not fetched/compared for this reason — revisit once the Portfolio Detail data-fetch bug is fixed or a portfolio with holdings loads successfully. |
| Compare: Correlation Heatmap | `portfolios/screens/instrument_comparison_screen.dart` (tab) | **MISMATCH** (functional bug, not just a redesign gap) | Live screenshotted 2026-09-02: reached via the "Compare instruments" icon on the Invest screen's top-right (content-desc "Compare instruments"). Live is a single screen with 4 tabs — Summary, Risk metrics, Correlation, Factsheet — and a shared ticker-search/chip row (added AAPL + MSFT to populate data) and a 1y/%/Abs control row above the tab body. Stitch mock ID `b68c90c2fc93466386edea741720ba64`, title "Compare: Correlation Heatmap", shows a distinct "Markets" header (search icon, no back arrow) and a proper correlation matrix: a table with tickers on both axes, each cell a coloured percentage (dark green 1.00 diagonal, green/pink gradient by correlation strength, red for negative), and an "Inverse ↔ Aligned" gradient legend below. **Live's Correlation tab does not render a heatmap or matrix at all — it shows the exact same overlaid price-history line chart as the Summary tab** (confirmed by switching between Summary/Risk metrics/Correlation/Factsheet tabs with the same two tickers loaded: all four tabs render pixel-identical content, only the tab label changes). This is a real implementation gap, not cosmetic — the correlation-specific visualization described in the Stitch mock isn't built; the tab is present in the UI but wired to the same body as every other tab. Flag for a code check outside this documentation-only pass. Also noted in passing: a Flutter debug-mode "BOTTOM OVERFLOWED BY 1.9 PIXELS" banner appears at the bottom of the chart area whenever the ticker-search keyboard is open on this screen — a real (if minor, debug-only) layout overflow bug, reproducible by focusing the ticker field with 2 tickers already added. |
| Compare: Factsheets | `portfolios/screens/instrument_comparison_screen.dart` (tab) | **MISMATCH** (same functional bug as Correlation — Factsheet tab is an unwired stub) | Live screenshotted 2026-09-02, same Compare screen/session as the Correlation row above (AAPL + MSFT loaded). Stitch mock ID `272ab0a2cea148aba3cc2503355e9860`, title "Compare: Factsheets", shows yet another distinct chrome — a "Piggybank" header, ticker chips with an "+A" quick-add, a Performance/Factsheet/Holdings tab row, and per-instrument fund-fact cards (Fund Family, Category, Expense Ratio, Dividend Yield, Inception Date) for each selected ticker (mock uses STX40/SYGE40/CTOP50, ETF tickers, not AAPL/MSFT — expected, different demo tickers). Live's Factsheet tab shows the identical overlaid price-history line chart as every other tab — no fund-fact cards, no per-instrument data rows at all. Same root cause as the Correlation tab: this tab isn't wired to distinct content. Flag alongside the Correlation row for a single code check — likely one shared bug (probably a `TabBarView` or `IndexedStack` misconfiguration serving the same child for every tab) rather than four separate missing features. |
| Compare: Risk Metrics (Data Scannability) | `portfolios/screens/instrument_comparison_screen.dart` (tab) | **MISMATCH** (same functional bug as Correlation/Factsheet — Risk metrics tab is an unwired stub) | Live screenshotted 2026-09-02, same Compare screen/session (AAPL + MSFT loaded). Stitch mock ID `b9bbd73cc63d447080fc9031cef1a374`, title "Compare: Risk Metrics (Data Scannability)", shows a "Compare" header with search icon, ticker chips, a "Performance Over Time" line-chart card with 1M/1Y/3Y range toggle, a Summary/Risk metrics segmented control, and a per-instrument risk-metrics card list (Annualised return, Volatility (ann.), Max drawdown, Sharpe ratio — one card per ticker, coloured red for negative drawdown). Live's Risk metrics tab shows the identical overlaid price-history line chart as every other tab — no risk-metric cards, no annualised return/volatility/drawdown/Sharpe figures anywhere. Confirms the pattern seen on Correlation and Factsheet: all 4 tabs (Summary, Risk metrics, Correlation, Factsheet) render pixel-identical content on live; only the Summary tab's chart view is actually implemented, and the other three tabs are stubs that happen to share it. Flag alongside the other two Compare-tab rows for one code check. |
| Expenses Summary | `expenses/screens/expenses_summary_screen.dart` | **MISMATCH** (redesign gap, not a bug) | Live screenshotted 2026-09-03 (demo account), reached via the pie-chart/breakdown icon in the Transactions screen header (content-desc "Expenses summary"). Stitch mock ID `ccc4ce36b3be466682edac25b85d0b5e`, title "Expenses Summary" confirmed via `get_screen`. Both are a total-spend hero card + category list, but differ in almost every specific: (1) Stitch's hero card is scoped to a month ("Total Spent (May)") with a "↓ 8.5% from April" trend pill; live's hero card just says "Total" with no month/trend comparison, R 143 810,00 (all-time/all-transactions total, not month-scoped); (2) live has a segmented multi-shade-green progress bar directly under the hero card (implicitly showing category proportions) that Stitch doesn't have; (3) section heading — Stitch "Category Breakdown" vs. live "By category"; (4) row content differs — Stitch rows show a category icon (cart/car/fork-knife/lightning/dots), amount, a per-row green progress bar, and a percentage label; live rows show a plain coloured dot (not a category-specific icon), a transaction-count subtitle (e.g. "16 transactions") instead of a percentage, and no per-row progress bar at all; (5) category set differs — Stitch shows 5 categories (Groceries, Transport, Dining Out, Utilities, Other-catchall); live shows 9 named categories (Rent, Groceries, Insurance, Transport, Petrol, Utilities, Entertainment, Clothing, Dining Out, Medical, scrollable) with no "Other" catch-all row. Functionally live works correctly (real totals, real per-category transaction counts) — this is a cosmetic/structural redesign gap, not a broken screen. |
| Insights | `insights/screens/insights_screen.dart` | **MISMATCH** (redesign gap — different feature paradigm, not just cosmetic) | Live screenshotted 2026-09-02 (demo account, Insights bottom-nav tab). Stitch mock ID `ea7f15dbe6964a239590041661ca1ece`, title "Insights" confirmed via `get_screen`. The two are structurally different features, not just re-skinned: Stitch is a static dashboard — a green gradient "Net Worth Trend" hero card (value + "+2.4% this month" pill), a "Spending Patterns" section with two comparison cards (Groceries "15% higher than last month" R4,200, Entertainment "5% lower than last month" R1,850), a "Savings Rate" card (current 18% vs. "Target: 20%" progress bar + encouragement copy), and a "Projected Wealth" card ("In 6 Months" R142,000 + trend icon). Live is instead a conversational AI-assistant interface: a text input ("Ask about your spending, budgets, or net worth…") with a send arrow, and a "Past insights" list of previously-asked queries with timestamps and truncated answers (e.g. "Your net worth is R1,808,030.50." at 2026-09-01 10:12:52). None of Stitch's specific cards/metrics (net worth trend chart, spending pattern comparisons, savings rate vs. target, projected wealth) appear on live in that card form — live surfaces the same kind of information (net worth, spending) only as freeform chat answers a user has to ask for, not as an always-visible dashboard. Header also differs: Stitch has a mascot avatar + small logo glyph + bell; live has a green person-icon avatar, "Insights" title, and a bell. This looks like a deliberate pivot to a chat/assistant paradigm rather than an unfinished build of the Stitch dashboard, but flag for confirmation since it's a significant product-direction difference, not a minor visual one. |
| Loan Calculator | `calculators/screens/calculators_screen.dart` | **MISMATCH** (redesign gap, and neither Stitch mock is an exact match) | Live screenshotted 2026-09-03 (demo account), reached via the Home screen's "Calculators" quick-action tile. Live is a single "Calculators" screen with a green segmented "Loan Calculator / Loan Accelerator" toggle at the top (not two separate screens), a subtitle "Work out the monthly payment for a loan", and three plain bordered text inputs (Loan amount (ZAR), Annual interest rate (%), Term (months)) plus a full-width "Calculate" button; tapping Calculate renders a single green gradient "Monthly payment" result card with just one large figure — no breakdown stats, no Apply/CTA button. Checked both duplicate-titled Stitch mocks for this row (project `4011413732038259168`): ID `a30b1ffd41c4496c9e0033e16a618d39` — "Loan Calculator" heading, subtitle "Estimate your monthly payments instantly.", card-based inputs (Loan Amount/Interest Rate/Loan Term with 12m/24m/36m/60m quick-pick pills), and a green result card with three stats (Estimated Monthly Payment + Total Interest + Total Repayment) plus a dark-green "Apply for Loan →" button — no bottom nav. ID `5dd86a95f8564bd6a3e71fe1ee273a78` — same heading, subtitle "Estimate your monthly repayments.", result card shown *above* the inputs, slider-based inputs (Loan Amount/Interest Rate/Loan Term in years, with 1/3/5/10-year quick buttons), "Apply for Loan" button, and includes bottom nav. Neither mock has the Loan Accelerator toggle live combines into the same screen, and neither uses live's plain-text-field/months-based input style — both use sliders or quick-pill amounts and richer multi-stat result cards with an Apply CTA that live's Calculate result lacks entirely. This reads as an earlier/alternate Stitch iteration predating the live app's simpler combined-screen redesign, not a 1:1 target. Functionally the live calculator computes and displays a monthly payment correctly. |
| Loan Accelerator | `calculators/screens/calculators_screen.dart` (accelerator mode/tab) | **MISMATCH** (see Loan Calculator row — same combined screen, no distinct Stitch mock) | Live screenshotted 2026-09-03, the "Loan Accelerator" segment of the same Home → Calculators screen described above (not a separate route). Subtitle "See how much extra monthly payments save"; four plain bordered text inputs (Outstanding balance (ZAR), Annual interest rate (%), Remaining term (months), Extra monthly payment (ZAR)) plus a "Calculate" button. No Stitch mock exists for this specific accelerator-savings screen — the two Stitch IDs checked for "Loan Calculator" (see above) are both single-purpose monthly-payment calculators with no extra-payment/acceleration inputs or savings-comparison output, so there is nothing to compare this row against directly; flagging as NO STITCH-equivalent-found rather than a true mismatch. |
| Import history | `settings/screens/import_history_screen.dart` | **MISMATCH** (redesign gap, not a bug) | Live screenshotted 2026-09-03, reached via Settings → Import history. Stitch mock ID `0dbf66df217446728adbf2b94a7d4848`, title "Import history" confirmed via `get_screen`. Both are a plain vertical list of past import batches with status pills, but differ in detail: Stitch shows 5 rows with a distinct icon per file type (green doc icon for CSV, orange/yellow icon for a differently-typed file) and 3 status states (green "Success", orange "Processing", red "Failed"); live shows every row with the same green doc icon regardless of file, only 2 status states appear in the demo data (green "COMPLETED", red "FAILED" — no "Processing" state observed), and live's subtitle format is "`X / Y imported`" (plus "`· N failed`" when applicable) vs. Stitch's looser "428 / 430 imported · 2 failed" / "Processing 150 rows..." phrasing. Live's demo data includes several QA/test filenames (bad_import.csv, qa_import_test.csv ×2, seed.csv, verify.csv ×2) rather than Stitch's realistic bank-statement filenames (StandardBank_Oct2..., PayPal_Q3.csv, Amex_Sep23_Corrupted...) — expected, just demo-data flavour, not a mismatch. Functionally live works correctly (accurate imported/failed counts per row); this is a cosmetic/data-flavour difference, not a broken screen. |
| Settings | `core/router/placeholder_screens.dart`'s `SettingsScreen` | **MISMATCH** (redesign gap, and live is missing a feature Stitch has) | Live screenshotted 2026-09-03. Stitch mock ID `a2a858dd98c741f5b6ef7b1f88c5d177`, title "Settings" confirmed via `get_screen`. Live: a "Demo User" account card (avatar/name/email, no plan badge), a standalone "Privacy & consent" card, then one grouped card with 6 plain rows (Subscription, Import history, Appearance, Security, Notifications, AI Assistant), each with a green circular icon and a chevron. Stitch: profile card includes a green "PREMIUM" pill badge next to the name (live's account card has no plan-status badge at all — Pro status is only visible one screen deeper, in Subscription), and rows are organised into 4 labelled sections (Account: Personal Profile/Subscription/Linked Accounts; Preferences: Notifications/Appearance/Privacy & Consent; Security: Biometric Lock toggle inline/Change PIN; Support: Help Center/Contact Us/About Piggybank with version number) — live has no section labels, no "Personal Profile" row, no "Linked Accounts" row, no Support section (Help Center/Contact Us/About/version are all absent from live), and folds Privacy & consent into its own standalone card instead of a Preferences-section row. The doc's own note about `SettingsScreen` being a "real implementation despite the filename" is confirmed — it's fully functional, just structurally simpler than the Stitch mock and missing the Linked Accounts and Support-section features entirely (not merely relabeled). |
| Appearance Settings | `settings/screens/appearance_screen.dart` | **MATCH** (near-identical) | Live screenshotted 2026-09-03. Stitch mock ID `5c3f9b44f15f46258bbe436f7236a11f`, title "Appearance" confirmed via `get_screen`. Both are a simple radio-button list of System/Light/Dark theme options. Live orders them System → Light → Dark (System selected, matching demo state) and gives each a subtitle ("Match your device setting" / "Always use the light palette" / "Always use the dark palette"); Stitch's fetched markup didn't clearly return subtitle text or confirm row order, and uses a slightly different icon set (a contrast icon for System vs. live's sun-with-halo glyph), but the core structure — 3 radio rows, one card, no other content — matches. Closest 1:1 match found so far in this sweep. |
| Security Settings | `settings/screens/security_screen.dart` | **MISMATCH** (redesign gap — live is simpler and adds account-management rows Stitch doesn't have) | Live screenshotted 2026-09-03. Stitch mock ID `0e6d79d1dab1416db58c5e9e6a7966d0`, title "Security" confirmed via `get_screen`. Live: one card with "Biometric unlock" (toggle, ON — explains the earlier session's stuck-lock-screen issue, since no fingerprint is actually enrolled on the emulator) + "Set PIN", an explanatory note ("Your biometric data is encrypted and stored locally on your device. We never send it to our servers."), then a second card with "Export my data" and a red-icon "Delete my account" row. Stitch: 4 flat rows with no card grouping or explanatory note — "Use Face ID or Fingerprint to sign in" (fingerprint icon), "Change PIN", "Change password", "Remove PIN" — splitting PIN management into 3 separate rows where live has just one "Set PIN" row, and Stitch has no data-export or account-deletion rows at all. Functionally live's biometric toggle and Set PIN are real controls; not evaluated further (no code-check needed, this is a structural/content difference only). |
| Notifications Settings | `settings/screens/notifications_screen.dart` | **MATCH** (near-identical) | Live screenshotted 2026-09-03. Stitch mock ID `cf349f57487c4d66ac8e938f0af03a41`, title "Notifications" confirmed via `get_screen`. Both show exactly two toggle rows in one card — "Budget alerts" ("When a budget category is close to its limit") and "Weekly summary" ("A weekly recap of spending and progress") — live has both toggled ON in the demo account. Structure, copy, and row count match; no other differences observed. |
| Subscription Settings | `settings/screens/subscription_screen.dart` | **MISMATCH** (different plan state, not a bug — Stitch shows the free/upsell state, live shows the active-Pro state) | Live screenshotted 2026-09-03. Stitch mock ID `5af9aee13f844f58801b5ee0eac28e30`, title "Subscription" confirmed via `get_screen`. Live: "CURRENT PLAN" section header, a Pro plan card (green "PRO" badge, "Active · renews 2026-09-27"), a "What Pro unlocks" bullet list (3 checkmarked items: Unlimited accounts & portfolios, AI-powered Insights, AI Chatbot), and an outlined "Cancel subscription" button — this is the active-subscriber view, consistent with the demo account's known Pro-tier status (see memory: demo account is Pro tier). Stitch: shows the opposite state — a "Free" badge/"Active" status card, an "Unlock Premium" upsell card with the same 3-bullet feature list (worded "Personal Financial Assistant" instead of live's "AI Chatbot" for the third item) and a solid "Upgrade to Pro" CTA with a forward arrow — no "Cancel subscription" button, since this is the pre-upgrade state. Both states are plausible views of the same screen depending on account tier; recommend checking whether a Stitch mock of the active/Pro state exists elsewhere in the project, but as fetched, the two are simply showing different data states of a matching screen concept, not a structural mismatch. |
| Financial Assistant: Welcome | `chatbot/screens/chatbot_screen.dart` (empty state) | **MATCH** (near-identical, minor copy differences) | Live screenshotted 2026-09-03, reached via Settings → AI Assistant. Stitch mock ID `6043eadddea54a93a861bca2b8eeffa8`, title "Financial Assistant: Welcome" confirmed via `get_screen`. Both show a centered welcome state with a mascot/piggy icon, a heading, a subtitle about tracking spending/budgets/investments, and the same 3 suggestion chips verbatim ("How much did I spend on dining?", "Am I on track with my budget?", "Show my net worth trend"), plus a bottom text-input bar with a send button. Minor differences: live's header shows a "Penny" avatar + name + "Ask me anything about your money" subtitle (a named-mascot branding), while Stitch's heading reads "Hi, I'm your financial assistant" (generic, unnamed) with a slightly longer subtitle ending "...What's on your mind?"; live's heading is "Hi, I'm Penny". Structurally identical (icon → heading → subtitle → 3 chips → input bar); only the assistant's naming/copy differs. |
| Financial Assistant: Chat Thread | `chatbot/screens/chatbot_screen.dart` (with messages) | **MATCH** (structurally identical, sample content differs as expected) | Live screenshotted 2026-09-03, same session as Welcome above — sent "How much did I spend on dining?" via a suggestion chip. Stitch mock ID `ccb0d51be8c14c2f9decad3efb3211b1`, title "Financial Assistant: Chat Thread" confirmed via `get_screen`. Both use the same bubble convention: user messages right-aligned in a solid dark/green bubble, assistant replies left-aligned in a light bubble. Live's actual exchange ("How much did I spend on dining?" → "You spent R 700.00 on dining out this month.") is plain text; Stitch's sample conversation is a different Q&A ("How's my spending looking for May?" / groceries budget) and includes a richer embedded data card in one assistant reply (a "Total Spent R 4,230, 12% lower" stat block with a green down-arrow) that live's plain-text reply doesn't show for this particular query — worth noting live *can* render structured content elsewhere (e.g. Insights' "Past insights" list), so this may just be a different query type than what was tested live, not a missing feature. Header also matches in spirit: Stitch's "Piggybank - AI Financial Assistant" + tagline vs. live's "Penny" + "Ask me anything about your money" — same named-vs-generic branding gap noted in the Welcome row. |
| Imports Wizard | `imports/screens/imports_screen.dart` | **MATCH** (close match to one of the two duplicate Stitch mocks) | Live screenshotted 2026-09-03, reached via Transactions header's "Import CSV / Scan receipt" icon (content-desc confirmed). Live's "Imports" screen: a "Scan receipt" section (Take photo / Pick image buttons, "or choose a PDF instead" link), then an "Import CSV" section with a 3-step segmented control (1 Configure / 2 Upload / 3 Review, currently on step 1), a "Bank template" dropdown ("None (generic)"), an "Account (optional)" dropdown ("No account"), a green "Continue" button, and a "Past imports" list below with status pills. Checked both duplicate-titled Stitch mocks (project `4011413732038259168`): ID `86ee7f0ab6db4cbb96280e744b7eadd7` is the close match — same Scan Receipt section (Take photo/Pick image + "or choose a PDF instead"), and explicitly labels the same 3-step "Configure / Upload / Review" CSV sequence live uses, plus a themed results/preview stage (Date/Description/Amount table, success checkmark, import-summary stats, "Normalize with AI" button — this richer step-3 content wasn't reached live since the demo run stayed on step 1) and a "Past Imports" list with named batches and fraction-imported status, matching live's pattern closely. ID `ee27b2f8cc6d49da956c7fdb169c5df0` is the mismatch/alternate: full app chrome with bottom nav (live's Imports screen has none), a single-step CSV flow with bank-selection chips (Standard Bank/FNB/Absa/Capitec) and inline file preview instead of the 3-step Configure/Upload/Review control, and a receipt-scan flow that includes an OCR "Review Scanned Data" panel with a confidence score — features live's step-1 view doesn't show. Recommend treating `86ee...` as the canonical target for this screen and flagging `ee27...` as either an earlier iteration or a distinct/rejected design direction. |
| Admin Dashboard | — | **NO FLUTTER** | See Step 1. |
| User Management | — | **NO FLUTTER** | See Step 1. |

**Flutter screens with no Stitch mock found at all** (register, TFSA/RA ledgers, data export,
portfolio/holding edit sheets) — not this pass's focus (comparing Stitch → live), but noted:
`auth/screens/register_screen.dart`, `tfsa/screens/tfsa_ledger_screen.dart`,
`ra/screens/ra_ledger_screen.dart`, `settings/screens/data_export_screen.dart`,
`portfolios/screens/add_edit_holding_sheet.dart`, `portfolios/screens/portfolio_sheet.dart`,
`portfolios/screens/sell_holding_sheet.dart`, `portfolios/screens/all_holdings_screen.dart`.

## Step 3 — Live comparison log

Sweep paused after the mascot fix (2026-09-01) — see the scope-pivot note at the top. Remaining
rows in Step 2's table are still `TBD`. Resume by picking an explicit baseline first (current
exploratory Stitch project as a redesign target, vs. the older `docs/ui-ux-mockup-brief.md`
mockups the app already targets) rather than assuming the current Stitch project is "correct."

**Verification for this pass's fix:**
- `flutter analyze` — No issues found.
- `flutter test` — 425/425 passing (full suite, includes `lock_screen_test.dart` 5/5).
- Live: Login and Register screenshotted on a fresh debug-APK install/reinstall against a
  clean emulator (`pm clear`, fresh launch) — mascot renders correctly, properly sized and
  centered on both. Lock Screen not live-screenshotted this session (see table note above).

## Step 4 — Triage Punch List

Triage of every MISMATCH row above, done 2026-09-03, grouping them by what (if anything) to
do next. This is the reference list for a future code-check pass — read this instead of
re-reading all 20+ verbose Step 2 notes.

### Bucket 1 — Actual bugs (feed into the code-check pass)

1. **Portfolio Detail / Invest — data-fetch error on non-empty portfolios** (highest
   priority — blocks 2 other rows). 2 of 3 demo portfolios (QA RA, Growth) show "Something
   went wrong" on Portfolio Detail; Invest tab's top section (hero card, P&L/dividends,
   allocation donut) shows the same error banner instead of rendering — almost certainly one
   shared root cause (whatever loads a portfolio's holdings). Also blocks live verification
   of **Holding Detail Sheet** (can't reach it — no portfolio with holdings loads).
2. **Compare screen — Correlation/Factsheet/Risk-metrics tabs are unwired stubs.** All 4 tabs
   (Summary/Risk metrics/Correlation/Factsheet) render pixel-identical content; only Summary
   is actually implemented. Likely one `TabBarView`/`IndexedStack` misconfiguration, not 3
   separate missing features.
3. **Compare screen — minor debug-only layout overflow.** "BOTTOM OVERFLOWED BY 1.9 PIXELS"
   banner when the ticker-search keyboard is open with 2+ tickers added. Low priority,
   debug-mode only.
4. **Forgot Password / Reset Password — missing mascot image entirely.** Unlike
   Login/Lock/Register (already fixed this same way), these two screens never had
   mascot-image code at all — no placeholder comment, just empty space. Same fix pattern as
   the already-completed mascot wiring; cheap, mechanical fix.
5. **Liabilities — duplicate list entries** (FNB Credit Card, Toyota Fortuner Finance, Home
   Loan-FNB each appear twice). Flagged in the doc as "likely a demo-data seeding artifact"
   rather than a code bug — worth a quick check of the demo seed script, lower priority than
   1-2.

### Bucket 2 — Worth raising with design/product (not a code fix)

- **Settings — missing entire sections vs. Stitch mock**: no Personal Profile row, no Linked
  Accounts row, no Support section (Help Center/Contact Us/About/version) at all. Could be
  deliberate current-scope-only, or a real gap — needs a product decision, not a blind fix.
- **Insights — paradigm shift, not just a redesign gap.** Stitch shows a static metrics
  dashboard (net worth trend, spending pattern cards, savings rate vs. target, projected
  wealth); live is a conversational AI-assistant/chat interface instead. Already flagged
  above as "looks like a deliberate pivot" — worth a one-line confirmation from the
  user/product owner rather than treating it as unfinished work.
- **Subscription — no Pro-state Stitch mock found.** Only the free/upsell-state mock exists;
  live's Pro-tier view has nothing to compare against. Worth a quick check (open item carried
  over from the previous session) before closing the row out for good.
- **Accounts — "Inactive accounts" section entirely absent from live** (Stitch has one).
  Could be unimplemented scope rather than a redesign-cosmetic choice — worth confirming
  intent.

### Bucket 3 — Cosmetic-only, no action needed

Everything else verdicted MISMATCH, explicitly tied to the unshipped exploratory Stitch
redesign per the scope-pivot note at the top of this doc — Login, Dashboard, Accounts (list
styling), Add account, Transactions, Add transaction, Budgets, Goals, Assets, Liabilities
(icon/colour styling, excluding the duplicate-entry bug above), Expenses Summary, Loan
Calculator, Loan Accelerator, Import history, Security Settings, Privacy & Consent (First-run
Gate). No action needed on any of these unless/until the exploratory redesign is formally
adopted as the new target.

Rows already verdicted **MATCH** need no action either — confirmed close/exact match:
Appearance Settings, Notifications Settings, Financial Assistant: Welcome, Financial
Assistant: Chat Thread, Imports Wizard, Privacy & Consent: Review (Settings).
